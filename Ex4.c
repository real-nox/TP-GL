#include <stdio.h>

int main() {
    printf("La  taille en octets d'un entier (int) est : %d\n", sizeof(int));
    
    printf("La  taille en octets d'un short entier (short int) est : %d\n", sizeof(short int));

    printf("La  taille en octets d'un long entier (long int) est : %d\n", sizeof(long int));

    printf("La  taille en octets d'un reel (float) est : %d\n", sizeof(float));

    printf("La  taille en octets d'un double (double) est : %d\n", sizeof(double));

    printf("La  taille en octets d'un long double (long double) est : %d\n", sizeof(long double));

    printf("La  taille en octets d'un caractere (char) est : %d\n", sizeof(char));
}